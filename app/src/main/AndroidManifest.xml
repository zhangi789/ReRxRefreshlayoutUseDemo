<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shanutec.cn">


    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".api.TkApplication"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



















        <activity android:name=".ui.MusicActivity" />
        <activity android:name=".ui.FoodActivity" />
        <activity android:name=".ui.ScienceActivity" />
        <activity android:name=".ui.PhotoActivity" />
        <activity android:name=".ui.StoryActivity" />
        <activity android:name=".ui.WebActivity"/>
        <activity android:name=".ui.NestedLayoutActivity"/>
        <activity android:name=".ui.CoordinateActivity"/>
        <activity android:name=".ui.NormalViewActivity"/>
        <activity android:name=".ui.TestActivity"/>
    </application>

</manifest>